import React, { ReactElement } from 'react'
import { View } from 'react-native'

import { GroupChannelSettingsProps } from '@sendbird/uikit-react-native'

const GroupChannelSettingsInfo = (
  _: GroupChannelSettingsProps['Info']
): ReactElement => {
  return <View />
}

export default GroupChannelSettingsInfo
